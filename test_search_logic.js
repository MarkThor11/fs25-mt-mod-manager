const scraper = require('./src/main/services/scraper'); async function testSearch() { const query = process.argv[2] || 'John Deere'; console.log('TESTING SEARCH FOR: ' + query); try { const result = await scraper.searchMods(query); console.log('FOUND ' + result.mods.length + ' MODS. Total pages: ' + result.totalPages); result.mods.forEach((m, i) => { console.log('[' + (i+1) + '] ID: ' + m.modId + ' | TITLE: ' + m.title + ' | AUTHOR: ' + m.author + ' | RATING: ' + m.rating); }); } catch (err) { console.error('SEARCH TEST FAILED:', err); } } testSearch();
